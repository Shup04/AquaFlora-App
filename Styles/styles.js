import React from 'react';
const styles = StyleSheet.create({
    header: {
      fontSize: 24,
      fontWeight: 'bold',
      margin: 10,
      marginTop: 20,
      color: Colors.textMarine,
    },
    listContainer: {
      width: '100%',
      minWidth: '100%',
    },
  });